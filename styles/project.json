{
  "name": "styles",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "styles/src",
  "projectType": "library",
  "targets": {
    "copyStylesToLessons": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["cp -R styles/src/** lessons/src/styles"],
        "parallel": true
      }
    }
  }
}
