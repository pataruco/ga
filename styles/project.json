{
  "name": "styles",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "styles/src",
  "projectType": "library",
  "targets": {
    "copyStylesToLessons": {
      "command": "cp -R styles/src/** lessons/src/styles"
    },
    "copyStylesToFewd": {
      "command": "cp -R styles/src/** apps/fewd/styles"
    }
  }
}
